import{_ as a,o as t,c as s,ag as n}from"./chunks/framework.CMYkYJvz.js";const h=JSON.parse('{"title":"Rule Patterns","description":"Rule matching patterns and priority details.","frontmatter":{"title":"Rule Patterns","description":"Rule matching patterns and priority details.","editLink":false},"headers":[],"relativePath":"en/reference/rules/patterns.md","filePath":"en/reference/rules/patterns.md"}'),r={name:"en/reference/rules/patterns.md"};function o(c,e,p,i,l,d){return t(),s("div",null,[...e[0]||(e[0]=[n(`<blockquote><p>This page is automatically synced from <code>docs-en/rules/patterns.md</code>. Language: <strong>English</strong> | <a href="./../../../reference/rules/patterns">中文</a></p></blockquote><h1 id="rule-matching-patterns" tabindex="-1">Rule Matching Patterns <a class="header-anchor" href="#rule-matching-patterns" aria-label="Permalink to &quot;Rule Matching Patterns&quot;">​</a></h1><p>Rule patterns match requests by domain, IP, wildcard, path, regex, and negation.</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>example.com</span></span>
<span class="line"><span>example.com/api</span></span>
<span class="line"><span>*.example.com</span></span>
<span class="line"><span>**.example.com</span></span>
<span class="line"><span>/pattern/i</span></span>
<span class="line"><span>!*.internal.example.com</span></span></code></pre></div><p>Prefer the simplest pattern that expresses the target. Use regex only when domain, path, or wildcard matching is insufficient.</p><blockquote><p>⚠️ <strong>Negation (<code>!</code>) is non-functional at runtime (verified, 0.0.96).</strong> <code>matches_host()</code> for a <code>!X</code> pattern always returns <code>false</code>, so a standalone <code>!</code>-prefixed rule (e.g. <code>!www.example.com host://127.0.0.1</code>) matches no request and forwards nothing. Use <code>excludeFilter://</code> to subtract a subset from a positively matched set instead — e.g. <code>svc.test host://127.0.0.1 excludeFilter:///account</code> (verified: <code>/account</code> falls through to the real backend, other paths forward).</p></blockquote>`,6)])])}const m=a(r,[["render",o]]);export{h as __pageData,m as default};
